{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "scripts": {
    "semantic-release": "lerna-semantic-release pre && lerna-semantic-release post"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^3.3.0",
    "lerna": "^8.0.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
